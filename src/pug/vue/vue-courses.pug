#courses.box.container-class(style="position: relative;")
    div(is="course"
        v-for="(singleCourse, dataId, index) in sortedCourse"
        :single-course="singleCourse" :single-course-id="dataId" :key="dataId")
